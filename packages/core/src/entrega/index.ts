/**
 * Entrega (NAPO-005) — como a operação se anuncia ao cliente.
 *
 * Só formatação derivada de configuração (RN17). A decisão de quando se entrega
 * é do motor de disponibilidade; aqui é a frase que ela vira na tela.
 */
export { descreverCobertura, descreverDiasDeEntrega, descreverRaio } from './descricao';
export type { Cobertura } from './descricao';
